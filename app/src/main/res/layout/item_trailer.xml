<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:layout_margin="2dp"
    android:background="?attr/colorPrimaryDark">

    <ImageView
        android:id="@+id/imgThumbnail"
        android:layout_width="@dimen/thumbnail_width"
        android:layout_height="@dimen/thumbnail_height"
        android:contentDescription="@string/cnt_trailer_thumbnail"
        android:scaleType="center"
        tools:srcCompat="@drawable/placeholder_trailer" />

    <ImageView
        android:id="@+id/btnPlay"
        android:layout_width="@dimen/thumbnail_width"
        android:layout_height="@dimen/thumbnail_height"
        android:background="@drawable/background_click_selector"
        android:clickable="true"
        android:contentDescription="@string/cnt_play_button"
        android:focusable="true"
        android:scaleType="center"
        app:srcCompat="@drawable/ic_play" />

</FrameLayout>
